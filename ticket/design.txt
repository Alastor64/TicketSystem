外存：
bpt<username,index>"userIndex"O(10000*24)0.24M
#bpt<username,privilege>"userPrivilege"
//多一次寻址，但是degree大了
filer<class user>"userData"O(10000*124)1.24M
class user
	arraystring<21> username ,<31>password,<5*cn+1>name,<31>mailAddr
	int privilege
const cn=3 (UTF-8)

class train
class order
	int begin,end,num,index,timestamp;
class order_detailed
	int status;
	TrainID trainID;
	StationName begin,end;
	int leaveTime,arriveTime;
	int price;
	int num;
class leaveTrain
	int beginday,endday,trainIndex,leaveTime,subprice;
	<>==以trainindex为准
class arriveTrain
	int trainIndex,arriveTime,subprice;

filer<class train>"trainData"O(10000*4848)48.48M
BPT<trainID,int>"trainIndex"O(10000*32)0.32M
BPT<trainID,int>"releasedTrainIndex"O(10000*32)0.32M
BPT<pair<pair<day,int(trainIndex)>,int>>"seatIndex"O(1000000*12)12M
filer<int[100]>"seatData"O(1000000*400)400M
BPT<pair<int(trainIndex),int(seatNum)>>"releasedSeatNum";O(10000*8)0.08M
BPT<pair<  pair<day,int(trainindex)>,order  >"pendingOrder"O(1000000*28)28M
BPT<int(userindex),pair<int(-timestamp),int(orderindex)>>"orderindex"O(1000000*8)8M
filer<order_detailed>"orderdata"O(1000000*124)124M
BPT<stationName,leaveTrain>"leavetrain"O(10000*100*36)36M
BPT<stationName,arriveTrain>"arrivetrain"O(10000*100*36)36M



内存：
map<username,privilege>loggeduser记录登录状态
class command
	char*argument[]
	int timestamp

算法(在内存中)：
query_ticket 归并所有能从s出发的车次与能到t的车次
			1000000*O(车)
query_transfer 将所有能从s出发的车次与能到t的车次按中转站拆分，再按<中转站，到达/出发时间>排序，最后双指针
			10000*100*O(车)
			极限情况内存好像存不下
			或者用bpt在外存归并
seatIndex和seatData只存修改过的
