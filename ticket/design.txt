外存：
bpt<username,index>"userIndex"
#bpt<username,privilege>"userPrivilege"
//多一次寻址，但是degree大了
filer<class user>"userData"
class user
	arraystring<21> username ,<31>password,<5*cn+1>name,<31>mailAddr
	int privilege
const cn=3 (UTF-8)

class train
class order
	int begin,end,num,index;
class order_detailed
	int status;
	trainName trainID;
	stationName begin,end;
	int leaveTime,arriveTime;
	int price;
	int num;
class passTrain
	int d1,d2,trainindex,leaveTime,seatIndex,sumprice,preprice,subprice,stationpos;
	TrainID trainID

filer<class train>"trainData"
BPT<trainID,int>"trainIndex"
BPT<trainID,int>"releasedTrainIndex"
BPT<pair<pair<day,int(trainIndex)>,int>>"seatIndex"
filer<int[100]>"seatData"
BPT<pair<int,int>>"releasedSeatNum";
BPT<pair<  pair< pair<day,int(trainindex)>,timestamp >,request  >"pendingOrder"
BPT<int(userindex),int(requestindex)>"orderindex"
filer<order_detailed>"orderdata"
BPT<stationName,passTrain>"passtrain"



内存：
map<username,privilege>loggeduser记录登录状态
class command
	char*key[],*argument[]
	int timestamp

算法(在内存中)：
query_ticket 归并所有能从s出发的车次与能到t的车次
			1000000*O(车)
query_transfer 将所有能从s出发的车次与能到t的车次按中转站拆分，再按<中转站，到达/出发时间>排序，最后双指针
			10000*100*O(车)
			极限情况内存好像存不下
			或者用bpt在外存归并
seatIndex和seatData只存修改过的
